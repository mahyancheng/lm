# Agency release verification

## World 3 idle-grid cash guard

The 16-quarter `grid_developer` autopilot trace closes with 0.139 of its regenerated opening cash, stable production and revenue, positive equity, preserved PPE, and lower debt. The filed cash-flow rows attribute the decline to sustaining capital expenditure and scheduled debt principal; the larger early movements coincide with deferred-revenue unwind. The repair test therefore treats 0.13 as a cash-retention floor and separately requires positive equity, while retaining its live-output, administration, upper-growth, and cross-background spread guards.

| Acceptance case | Evidence |
| --- | --- |
| Six-sector research to launch | `productBlueprint.test.ts` exercises a research-backed launch for AI, robotics, manufacturing, energy, logistics, and consumer sectors. |
| Private accelerator supply | `ownedAcceleratorContracts.test.ts` and `integrationFinanceContracts.test.ts` cover accepted delivery, priority against spot demand, partial/default/cancel/expiry, replay, PPE and cash settlement. |
| World 3 direct hardware | `physicsAccountingRegression.test.ts` proves shocked manufacturer direct plus anonymous output is physically bounded and direct rolled COGS is booked once. |
| Private A→B→C chain | `physicsAccountingRegression.test.ts` proves nonzero resolved output with B×10≤A and C≤B in one quarter. |
| Board and finance authority | `boardAuthority.test.ts`, `validator.test.ts`, debt/equity mandate tests, and action coverage exercise exact authority and execution. |
| Canonical agency path | Canonical session and server agency integration tests cover queued company actions and canonical quarter resolution. `TalkPanel.deals.test.tsx` additionally verifies a plain business acceptance becomes an explicit player confirmation and then a delivered contract outcome. |

Recursive typechecking and the production build passed in this environment. Live Pi rollout and Claude/Pi provider availability were not reachable from this environment, so no live-provider claim is made.

## Final gates — 2026-09-07

The full suite verified 3,401 cases, plus the additional TalkPanel acceptance test: 3,402 unique verified cases. Recursive TypeScript checking passed.

Existing World 3 saves receive the bounded, idempotent accelerator-manufacturer migration only when Tessellate has the exact legacy two-line layout. It restores the owned node and accelerator product without resetting cash, plant, or the action log; it replays history, writes a final checkpoint, bumps canonical revision, and records deterministic `migration_applied` audit evidence.

Live Pi/Claude rollout remains unreachable from this environment.
