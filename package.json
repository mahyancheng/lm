{
  "name": "frontier-capital",
  "private": true,
  "version": "0.1.0",
  "description": "Frontier Capital — an LLM-powered AI corporate grand-strategy simulation. Deterministic world engine + Claude-driven agents, on Vercel and Supabase.",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm --filter @frontier/web dev",
    "build": "pnpm --filter @frontier/web build",
    "start": "pnpm --filter @frontier/web start",
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint"
  }
}
